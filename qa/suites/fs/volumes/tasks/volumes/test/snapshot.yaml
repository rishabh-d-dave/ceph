tasks:
  - cephfs_test_runner:
      fail_on_skip: false
      modules:
        - tasks.cephfs.volume.subvolume.test_snapsot
        - tasks.cephfs.volume.subvolume.test_snapsot_metadata
        - tasks.cephfs.volume.subvolume.test_group_snapshot
